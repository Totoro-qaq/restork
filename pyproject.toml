[project]
name = "restork-repository-tools"
version = "0.0.0"
description = "Private development helpers for the Rust Restork workspace."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
  "pillow>=11.3",
]
